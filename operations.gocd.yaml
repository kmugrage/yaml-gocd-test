pipelines:
  Staging:
    group: Operations

    materials:
      uat: 
        pipeline: UserAcceptance
        stage: Approved

    stages:
      - DeployApplications:
          jobs:
            deploy:
              tasks:
                - exec:
                    command: echo