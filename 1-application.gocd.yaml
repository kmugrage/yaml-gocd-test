pipelines:
  Application:
    group: Development

    materials:
      mygit:
        git: /vagrant/2016-workshop
    stages:
      - build:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo
      - unit_test:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo
      - package:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo