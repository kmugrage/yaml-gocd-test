pipelines:
  Application:
    group: Development

    materials:
      mygit:
        git: /vagrant/2016-workshop
    stages:
      - build:
          jobs:
            build:
              tasks:
                - exec:
                    command: rake build
      - unit_test:
          jobs:
            build:
              tasks:
                - exec:
                    command: rake unit_test
      - package:
          jobs:
            build:
              tasks:
                - exec:
                    command: rake package
                    