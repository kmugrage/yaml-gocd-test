pipelines:
  test:
    group: simple

    materials:
      mygit:
        git: https://github.com/kmugrage/webinar-code.git

    stages:
      - build:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo

      - unit_test:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo

      - package:
          jobs:
            build:
              tasks:
                - exec:
                    command: echo


