pipelines:  
  FunctionalTesting:
    group: Development

    materials:
      services:
        pipeline: Application
        stage: package

    stages:
      - DeployApplications:
          jobs:
            deploy:
              tasks:
                - exec:
                    command: echo
      - FunctionalTests:
          jobs:
            runTests:
              tasks:
                - exec:
                    command: echo

  UserAcceptance:
    group: Development

    materials:
      services:
        pipeline: Application
        stage: package

    stages:
      - DeployApplications:
          jobs:
            deploy:
              tasks:
                - exec:
                    command: echo
      - Approved:
          approval:
            type: success
          jobs:
            approve:
              tasks:
                - exec:
                    command: echo

